{
  "name": "functions",
  "description": "Cloud Functions for Facture Zen — server-side RevenueCat entitlement verification",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  }
}
